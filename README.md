 O que eu fiz

- Criei uma **estrutura completa de site** com `header`, `main` e `footer`.
- No **header**, adicionei um menu de navegação com fundo em **gradiente cinza metálico** e **textos estilizados**.
- No **main**, dividi a tela em duas partes:
  - Lado **esquerdo**: contém os textos principais e meu nome destacado em vermelho.
  - Lado **direito**: exibe uma imagem central grande (`#minhaFoto`).
- No **footer**, adicionei um botão estilizado e um gradiente que combina com o topo do site.
- Implementei **efeitos interativos** nos ícones e imagens usando **JavaScript** e **CSS**.
